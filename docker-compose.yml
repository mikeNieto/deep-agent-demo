services:
  api:
    build: .
    container_name: gemini-live
    ports:
      - "${API_PORT:-8000}:${API_PORT:-8000}"
    env_file:
      - .env
    environment:
      - API_HOST=0.0.0.0
    restart: unless-stopped
